import { blocksRouter as blocks } from "./block"
import { foldersRouter as folders } from "./folder"
import { notesRouter as notes } from "./note"
import { projectsRouter as projects } from "./project"

export const router = { blocks, folders, notes, projects }
